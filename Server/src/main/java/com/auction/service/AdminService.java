package com.auction.service;

public class AdminService {
    //private LogDAO logDAO;
    //private UserDAO userDAO;

    // 1. [HÀM CỦA ADMIN] - Lấy lịch sử hệ thống (Có phân trang)
    //public LogPageDTO getAdminLogsByPage(int page, int limit) { ... }

    // 2. Khóa mỏm/Cấm tài khoản (Đổi UserStatus thành BANNED)
    //public boolean banUser(String adminId, String targetUserId, String reason) { ... }

    // 3. Ghi Log (Các hàm Service khác khi làm gì quan trọng sẽ gọi hàm này)
    //public void recordAction(String adminId, String actionDetail) { ... }
}
