package com.auction.server.controllers;

import java.util.HashMap;
import java.util.Map;

public class AuthController {
    /*// Giả sử trong RegisterController.java [cite: 119]
    public void handleRegister() {
        Map<String, Object> data = new HashMap<>();
        data.put("username", txtUsername.getText());
        data.put("email", txtEmail.getText());
        // Thêm các thông tin riêng cho Seller nếu có

        try {
            // Nếu UI đang ở chế độ đăng ký Seller
            Seller newSeller = authService.register(UserRole.SELLER, data);
            System.out.println("Đăng ký thành công Seller: " + newSeller.getBusinessName());
        } catch (Exception e) {
            showErrorDialog(e.getMessage()); // [cite: 172]
        }
    }*/
}
